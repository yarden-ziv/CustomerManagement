@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="utf-8" />
    <title>ברוך הבא</title>

    <style>
        body {
            background-color: #1A3C6E; /* Dark blue */
            color: white;
            font-family: "Segoe UI", Arial, sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            animation: fadeIn 1.5s ease-out forwards;
        }

        /* Fade-in animation */
        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* LOGO — slow zoom-in + fade */
        .logo {
            width: 220px;
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeIn 1.2s ease-out forwards, zoomIn 5s ease-out forwards;
            animation-delay: 0.1s;
            filter: drop-shadow(0 0 6px rgba(0,0,0,0.3));
        }

        @@keyframes zoomIn {
            from {
                transform: scale(1);
            }

            to {
                transform: scale(1.10);
            }
            /* Slight zoom (10%) */
        }

        h1 {
            font-size: 38px;
            margin: 10px 0 5px 0;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards;
            animation-delay: 0.6s;
        }

        /* Orange divider line */
        .divider {
            width: 200px;
            height: 4px;
            background-color: #FF6A00; /* Phoenix orange */
            margin: 10px 0 20px 0;
            border-radius: 2px;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards;
            animation-delay: 1s;
        }

        p {
            font-size: 20px;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards, pulse 2.5s infinite ease-in-out;
            animation-delay: 1.3s;
        }

        /* Gentle pulsing for subtitle */
        @@keyframes pulse {
            0% {
                opacity: 0.6;
            }

            50% {
                opacity: 1;
            }

            100% {
                opacity: 0.6;
            }
        }

        /* Rotating loading spinner */
        .spinner {
            margin-top: 40px;
            width: 40px;
            height: 40px;
            border: 4px solid rgba(255, 255, 255, 0.2);
            border-top-color: #FF6A00; /* Orange top for Phoenix brand */
            border-radius: 50%;
            animation: spin 1s linear infinite;
            opacity: 0;
            animation: fadeIn 1.5s ease-out forwards, spin 1s linear infinite;
            animation-delay: 1.6s;
        }

        @@keyframes spin {
            from {
                transform: rotate(0deg);
            }

            to {
                transform: rotate(360deg);
            }
        }

    </style>

    <script>
        // Redirect after 4 seconds
        setTimeout(() => {
            window.location.href = '/Customers/List';
        }, 4000);
    </script>
</head>

<body>

    <!-- Your logo (replace filename if needed) -->
    <img src="~/images/fenix-logo.png" class="logo" />

    <h1>ברוך הבא למערכת ניהול לקוחות הבנקים</h1>

    <div class="divider"></div>

    <p>מיד תועבר אל רשימת הלקוחות</p>

    <div class="spinner"></div>

</body>
</html>
